<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('common.edit_link'), edit_book_path(@book) %> |
  <%= link_to t('common.back_to_books'), books_path %>
  <%= button_to t('common.destroy_book'), @book, method: :delete %>
</nav>
